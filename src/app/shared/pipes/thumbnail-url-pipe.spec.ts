import { ThumbnailUrlPipe } from './thumbnail-url-pipe';

describe('ThumbnailUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ThumbnailUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
